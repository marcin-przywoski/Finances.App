namespace Finances.App.Client.Models;

/// <summary>
/// Semantic color variants for the StatCard component's icon, resolved to
/// theme tokens by the .stat-icon-* CSS classes so they adapt to dark mode.
/// </summary>
public enum StatCardVariant
{
    Primary,
    Info,
    Warning,
    Success,
    Danger,
    Purple,
    Pink
}
